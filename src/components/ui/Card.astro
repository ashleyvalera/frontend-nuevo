---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<div class:list={["bg-white rounded-xl border border-slate-200 shadow-sm", className]}>
  <slot />
</div>